const apiRoutes = {
  login: '/api/v1/login',
  signup: '/api/v1/signup',
  data: '/api/v1/data',
};

const appRoutes = {
  loginUrl: '/login',
  signupUrl: '/signup',
  mainUrl: '/',
};

export default apiRoutes;
export { appRoutes };
