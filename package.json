{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "simple version of a Slack-alike chat",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "start-server -s ./frontend/build -a localhost",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "author": "Ilia Vazhenin",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^1.1.6"
  }
}
